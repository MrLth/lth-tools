export declare enum REQ_CFG {
    TIMEOUT = 8000,
    CACHE_TIME = 1000
}
export declare enum REQ_ERR {
    TIMEOUT = "TIMEOUT",
    CANCELED = "CANCELED",
    NETWORK = "NETWORK"
}
export declare enum REQ_RETRY {
    COUNT = 2,
    FIELD = "Axios_Retry_Count",
    INTERVAL = 1000
}
